<?php
    /*
     * Variáveis pré definidas ou Arrays SUPER_GLOBAIS
     * Variáveis de ambiente
     */
$nome = $_GET['a'];
$num = $_GET['b'];
var_dump($nome);
var_dump($num);

 /*A partir do ? no endereço da página
  * são definidas as variáveis para a $_GET
  * essas informações são chamadas de query strings
  */

